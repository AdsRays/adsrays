export * from './metaApi';

// --- TEMP stubs for legacy-ui build ---
export async function fetchCampaignAgeInsights(/* accountId, campaignId, accessToken */) {
  return [];
}
export async function fetchCampaignTargetAges(/* accountId, campaignId, accessToken */) {
  return [];
}
